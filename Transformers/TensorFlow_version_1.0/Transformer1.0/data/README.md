## English to Portuguese Data set

The files are pickle dictionary files. One for training (~50,000) and one for validation. 

The data is the same as in "import tensorflow_datasets as tfds". See code.
